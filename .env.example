# Sprint 01 — Core persistence (active)
POSTGRES_USER=earp
POSTGRES_PASSWORD=change-me-in-local-env
POSTGRES_DB=earp
POSTGRES_PORT=5432

VALKEY_PORT=6379

# Sprint 02 — FastAPI API Gateway (reserved)
# JWT_SECRET=
# API_GATEWAY_PORT=8000

# Sprint 03 — LiteLLM / vLLM (reserved)
# LITELLM_MASTER_KEY=
# LITELLM_PORT=4000
# VLLM_PORT=8001

# Sprint 04 — TEI / Qdrant (reserved)
# TEI_DENSE_PORT=8080
# TEI_SPARSE_PORT=8081
# QDRANT_HTTP_PORT=6333
# QDRANT_GRPC_PORT=6334

# Sprint 05 — Docling (reserved)
# DOCLING_API_PORT=5001

# Sprint 09 — NeMo Guardrails (reserved)
# GUARDRAILS_PORT=8010

# Sprint 11 — Langfuse (reserved)
# LANGFUSE_PORT=3000
